<% provide(:title, "Edit Article") %> 
<%= raw render "preview_textile.js"%>

<h1>Edit Article</h1>
<%= form_for(@article) do |f| %>
  <%= render 'fields', :f => f %>
  <article id="preview"></article>
  <%= f.submit "Update Article" %>
<% end %>