<li>
  <div class="title"><%= link_to article.title, admin_article_path(article) %></div>
  <div class="abstract"><%= article.abstract %></div>
  <div class="timestamp">
    Posted <%= time_ago_in_words(article.created_at) %> ago.
  </div>
</li>