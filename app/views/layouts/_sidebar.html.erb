<ul id="sidebar">
  <li id="users" <% if selected == 'users' %>class="selected"<% end %>>
    <span class="arrow"></span>
    <%= link_to "Users", admin_users_path, :id => "users" %>
  </li>

  <li id="articles" <% if selected == 'articles' %>class="selected"<% end %>>
    <span class="arrow"></span>
    <%= link_to "Articles", admin_articles_path, :id => "articles" %>
  </li>

  <li id="comments" <% if selected == 'comments' %>class="selected"<% end %>>
    <span class="arrow"></span>
    <%= link_to "Comments", admin_comments_path, :id => "comments" %>
  </li>
</ul>